name: ML Pipeline CI/CD

on: [push]

jobs:
  data_preparation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Préparation des données
        run: echo "Chargement et nettoyage des données diabète..."

  training:
    runs-on: ubuntu-latest
    needs: data_preparation
    steps:
      - uses: actions/checkout@v3
      - name: Entraînement du modèle
        run: echo "Entraînement du modèle de prédiction..."

  evaluation:
    runs-on: ubuntu-latest
    needs: training
    steps:
      - uses: actions/checkout@v3
      - name: Évaluation du modèle
        run: echo "Calcul des métriques (accuracy, F1...)..."
